//##ModelId=3DF799E40104
//#include "../aInput.h"

#pragma once
typedef double	real;
typedef unsigned int uint;
#define signum(f)( f >= 0 ? 1: -1)
#define max(a, b)  (((a) > (b)) ? (a) : (b)) 

//extern aInput io_interface;